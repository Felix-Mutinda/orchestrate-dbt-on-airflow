feature_platform:
  target: local
  outputs:
    local:
      type: duckdb
      # Resolves to absolute path in Cosmos, relative path locally
      path: "{{ env_var('DUCKDB_PATH', 'var/duckdb/feature_platform.duckdb') }}"
      threads: 4